name: shelf_router_macro
description: Experimental Dart package that is a thin wrapper for shelf_router using macros
version: 0.0.0-dev.7
repository: https://github.com/eEQK/shelf_router_macro
topics: [server, shelf, macros]

environment:
  sdk: ^3.5.0-164.0.dev

dependencies:
  collection: ^1.0.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  macros: ^0.1.0-main.5
  meta: ^1.15.0

dev_dependencies:
  test: any
  lints: any
  matcher: ^0.12.16+1
  mocktail: ^1.0.3
  json: ^0.20.2
